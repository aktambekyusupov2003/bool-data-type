/*
    Create function func
    check the whole number. Integers are 0 and a positive number.
    Args:
        a: int
    Returns:
        bool
    */

bool func(int a) {
  return ((a) == int);
}

void main() {
  // print(func(45));
  //  int double string
  int son = 5;
  print(son);
}
